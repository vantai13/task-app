# --- Terraform ---
# Local Terraform state, plugins, and provider files
.terraform/
# Lock file should usually be committed, but ignore if needed for specific workflows
# .terraform.lock.hcl
# Terraform state files (!!! CRITICAL TO IGNORE !!!)
terraform.tfstate
terraform.tfstate.*
# Terraform plan files (can contain sensitive info)
*.tfplan
# Variable files containing sensitive values (like passwords, ARNs)
*.tfvars
# Override files
override.tf
override.tf.json
*.override.tf
*.override.tf.json
# CLI configuration files
.terraformrc
terraform.rc

# --- Python ---
# Byte-compiled / optimized / DLL files
__pycache__/
*.pyc
*.pyo
*.pyd
# Virtual environment folders
.venv/
venv/
env/
# Environment variables file (often contains secrets)
.env
# Distribution / packaging
*.egg-info/
dist/
build/

# --- IDEs / Editors ---
.vscode/
.idea/

# --- OS generated files ---
.DS_Store # macOS
Thumbs.db # Windows

# --- Log files ---
*.log